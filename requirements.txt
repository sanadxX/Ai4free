# Web framework and extensions
Flask==3.1.0
flask-cors==4.0.1
Flask-Redis==0.4.0
Flask-SQLAlchemy==3.1.1

# Production WSGI server
gunicorn==23.0.0
gevent

# HTTP client and API helper libraries
requests==2.32.3
httpx
openai==1.61.1

# Core lib (ai4free + compatible webscout)
ai4free==0.7
webscout<2025.10

# Networking / bypass utils (used by webscout providers)
curl_cffi
cloudscraper
tls-client

# Parsing / helpers
beautifulsoup4
lxml
fake-useragent
loguru
termcolor
regex
pydantic
tqdm

# Data validation and environment management
marshmallow==3.26.1
python-dotenv==1.0.1

# Database ORM and adapter
SQLAlchemy
psycopg2-binary

# Utility libraries
tiktoken

# Firebase and Supabase integration
firebase-admin==6.3.0
supabase==2.1.0
